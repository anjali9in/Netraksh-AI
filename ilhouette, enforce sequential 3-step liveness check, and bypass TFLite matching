[33mcommit f8e946c6353ed3067ba602c349940694203f930d[m[33m ([m[1;32mmaster[m[33m)[m
Author: Aryaman Singha <aryamansingha60@gmail.com>
Date:   Thu Jun 4 19:34:06 2026 +0530

    fix: resolve liveness challenge simulation timer drift and merge face alignment updates

[1mdiff --git a/src/components/LiveScannerPanel.tsx b/src/components/LiveScannerPanel.tsx[m
[1mindex b0e384b..225ff47 100644[m
[1m--- a/src/components/LiveScannerPanel.tsx[m
[1m+++ b/src/components/LiveScannerPanel.tsx[m
[36m@@ -75,6 +75,8 @@[m [mexport function LiveScannerPanel({[m
   const isDetectingFrameRef = useRef(false);[m
   const sessionStartedRef = useRef(false);[m
   const isCapturingRef = useRef(false);[m
[32m+[m[32m  const lastChallengeIndexRef = useRef<number>(0);[m
[32m+[m[32m  const challengeTickCountRef = useRef<number>(0);[m
   const pulseAnim = useRef(new Animated.Value(1)).current;[m
   const hasRequestedPermission = useRef(false);[m
 [m
[36m@@ -213,6 +215,8 @@[m [mexport function LiveScannerPanel({[m
     startTimeRef.current = Date.now();[m
     faceDetectionTimeRef.current = null;[m
     consecutiveAlignedRef.current = 0;[m
[32m+[m[32m    lastChallengeIndexRef.current = 0;[m
[32m+[m[32m    challengeTickCountRef.current = 0;[m
     setFaceDetected(false);[m
     setAlignmentMessage('Position your face in the circle');[m
     setLivenessState(isEnrollmentMode ? null : livenessService.resetSession());[m
[36m@@ -390,9 +394,21 @@[m [mexport function LiveScannerPanel({[m
         return;[m
       }[m
 [m
[32m+[m[32m      // Track challenge ticks and reset on transition[m
[32m+[m[32m      if (state.currentChallengeIndex !== lastChallengeIndexRef.current) {[m
[32m+[m[32m        lastChallengeIndexRef.current = state.currentChallengeIndex;[m
[32m+[m[32m        challengeTickCountRef.current = 0;[m
[32m+[m[32m      } else {[m
[32m+[m[32m        challengeTickCountRef.current++;[m
[32m+[m[32m      }[m
[32m+[m
[32m+[m[32m      // 1 tick = 100ms, so tick count * 100 simulates milliseconds elapsed for the current challenge[m
[32m+[m[32m      const currentChallengeTimeMs = challengeTickCountRef.current * 100;[m
[32m+[m
[32m+[m[32m      // Simulate metrics dynamically according to current challenge type based on currentChallengeTimeMs[m
       const sim = livenessService.getSimulatedMetrics([m
         currentChallenge.type,[m
[31m-        challengeElapsed,[m
[32m+[m[32m        currentChallengeTimeMs,[m
       );[m
       setMetrics(sim);[m
 [m
[1mdiff --git a/src/config/thresholds.ts b/src/config/thresholds.ts[m
[1mindex 8d22d8b..0344905 100644[m
[1m--- a/src/config/thresholds.ts[m
[1m+++ b/src/config/thresholds.ts[m
[36m@@ -3,3 +3,8 @@[m
 export const FACE_MATCH_THRESHOLD = 0.68;[m
 export const BLINK_THRESHOLD = 0.22;[m
 export const MAX_AUTH_TIME_MS = 1000;[m
[32m+[m
[32m+[m[32m// Liveness detection thresholds[m
[32m+[m[32mexport const SMILE_THRESHOLD = 0.50;[m
[32m+[m[32mexport const HEAD_TURN_RIGHT_THRESHOLD = 0.60;[m
[32m+[m[32mexport const HEAD_TURN_LEFT_THRESHOLD = 1.60;[m
[1mdiff --git a/src/services/liveness/livenessService.ts b/src/services/liveness/livenessService.ts[m
[1mindex 7032c00..4e53dd2 100644[m
[1m--- a/src/services/liveness/livenessService.ts[m
[1m+++ b/src/services/liveness/livenessService.ts[m
[36m@@ -1,4 +1,9 @@[m
[31m-import {BLINK_THRESHOLD} from '../../config/thresholds';[m
[32m+[m[32mimport {[m
[32m+[m[32m  BLINK_THRESHOLD,[m
[32m+[m[32m  SMILE_THRESHOLD,[m
[32m+[m[32m  HEAD_TURN_LEFT_THRESHOLD,[m
[32m+[m[32m  HEAD_TURN_RIGHT_THRESHOLD,[m
[32m+[m[32m} from '../../config/thresholds';[m
 [m
 export type LivenessChallengeType = 'BLINK' | 'SMILE' | 'HEAD_TURN';[m
 [m
[36m@@ -230,7 +235,7 @@[m [mexport class LivenessService {[m
           this.blinkFramesCount++;[m
         } else {[m
           // If we had a closed eye for at least 1-2 frames and now it is open again, register a blink[m
[31m-          if (this.blinkFramesCount >= 1 && this.blinkFramesCount < 10) {[m
[32m+[m[32m          if (this.blinkFramesCount >= 1 && this.blinkFramesCount < 30) {[m
             currentChallenge.currentCount++;[m
             console.log([m
               `[LivenessService] Blink registered! Count: ${currentChallenge.currentCount}/${currentChallenge.targetCount}`,[m
[36m@@ -245,8 +250,8 @@[m [mexport class LivenessService {[m
         break;[m
 [m
       case 'SMILE':[m
[31m-        // Check for smile: MAR goes above 0.50 (wide mouth stretching)[m
[31m-        if (marVal > 0.5) {[m
[32m+[m[32m        // Check for smile: MAR goes above threshold (wide mouth stretching)[m
[32m+[m[32m        if (marVal > SMILE_THRESHOLD) {[m
           this.smileFramesCount++;[m
           if (this.smileFramesCount >= 3) {[m
             // held for 3 frames[m
[36m@@ -259,8 +264,8 @@[m [mexport class LivenessService {[m
         break;[m
 [m
       case 'HEAD_TURN':[m
[31m-        // Check for head turn: Yaw ratio < 0.60 (turn right) or > 1.60 (turn left)[m
[31m-        if (yawRatioVal < 0.6 || yawRatioVal > 1.6) {[m
[32m+[m[32m        // Check for head turn: Yaw ratio < right turn threshold or > left turn threshold[m
[32m+[m[32m        if (yawRatioVal < HEAD_TURN_RIGHT_THRESHOLD || yawRatioVal > HEAD_TURN_LEFT_THRESHOLD) {[m
           this.headTurnFramesCount++;[m
           if (this.headTurnFramesCount >= 3) {[m
             // held head turn for 3 frames[m
[36m@@ -304,35 +309,36 @@[m [mexport class LivenessService {[m
     challengeType: LivenessChallengeType,[m
     timeMs: number,[m
   ): {ear: number; mar: number; yawRatio: number} {[m
[31m-    let ear = 0.32 + Math.sin(timeMs / 100) * 0.02; // Baseline eye open[m
[31m-    let mar = 0.18 + Math.cos(timeMs / 150) * 0.02; // Baseline neutral mouth[m
[31m-    let yawRatio = 1.0 + Math.sin(timeMs / 200) * 0.05; // Centered face[m
[32m+[m[32m    const tick = Math.round(timeMs / 100);[m
[32m+[m[32m    let ear = 0.32; // Default open[m
[32m+[m[32m    let mar = 0.18; // Default neutral[m
[32m+[m[32m    let yawRatio = 1.0; // Default centered[m
 [m
[31m-    // Introduce movements corresponding to the active challenge[m
     switch (challengeType) {[m
       case 'BLINK':[m
[31m-        // Simulate a blink every 1.5 seconds[m
[31m-        const blinkCycle = timeMs % 1500;[m
[31m-        if (blinkCycle > 800 && blinkCycle < 1000) {[m
[32m+[m[32m        // We want to simulate a blink twice.[m
[32m+[m[32m        // First blink: eyes close at ticks 8-9 (800ms-900ms), open at tick 10 (1000ms)[m
[32m+[m[32m        // Second blink: eyes close at ticks 18-19 (1800ms-1900ms), open at tick 20 (2000ms)[m
[32m+[m[32m        if ((tick >= 8 && tick <= 9) || (tick >= 18 && tick <= 19)) {[m
           ear = 0.14; // Closed eye[m
         }[m
         break;[m
 [m
       case 'SMILE':[m
[31m-        // Simulate a smile building up over time[m
[31m-        const smileIntensity = Math.min(1.0, (timeMs % 3000) / 2000);[m
[31m-        if (smileIntensity > 0.4) {[m
[31m-          mar = 0.18 + (smileIntensity - 0.4) * 0.6; // Expands up to ~0.54[m
[32m+[m[32m        // Smile builds up: reach wide smile (>SMILE_THRESHOLD) by tick 10 (1000ms) and hold it[m
[32m+[m[32m        if (tick >= 10) {[m
[32m+[m[32m          mar = SMILE_THRESHOLD + 0.05; // 0.55[m
[32m+[m[32m        } else {[m
[32m+[m[32m          mar = 0.18 + (tick / 10) * (SMILE_THRESHOLD - 0.18 + 0.05);[m
         }[m
         break;[m
 [m
       case 'HEAD_TURN':[m
[31m-        // Simulate head turning left then right[m
[31m-        const turnIntensity = Math.sin(timeMs / 800); // oscillates between -1 and 1[m
[31m-        if (turnIntensity > 0.5) {[m
[31m-          yawRatio = 1.0 + (turnIntensity - 0.5) * 1.5; // Yaw goes up to 1.75 (left turn)[m
[31m-        } else if (turnIntensity < -0.5) {[m
[31m-          yawRatio = 1.0 + (turnIntensity + 0.5) * 0.9; // Yaw goes down to 0.55 (right turn)[m
[32m+[m[32m        // Turn head: reach turn (>HEAD_TURN_